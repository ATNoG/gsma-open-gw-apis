# QoD Demo Website

Run the app in development:

```bash
pnpm dev
```

Run the linter:

```bash
pnpm lint
```

Build the application (also checks types)

```bash
pnpm build
```

Preview or serve the app:

```bash
pnpm preview
```
