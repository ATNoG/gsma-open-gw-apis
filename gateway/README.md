Installing:

```sh
$ uv sync
```

Running the gateway:

```sh
$ uv run fastapi dev
```

Formatting:

```sh
$ ruff format
```

Linting:

```sh
$ ruff check && uv run mypy app
```

Running the tests:

```sh
$ uv run pytest
```
